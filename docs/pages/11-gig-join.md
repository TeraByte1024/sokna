# 11. 공연 참가 신청 다이얼로그 [회원 전용]

## 1. 개요 및 접근 권한
- **URL 경로**: `/gigs/[id]`의 `공연 참여` 버튼. 기존 `/gigs/[id]/join`은 `/gigs/[id]?join=true`로 리다이렉트.
- **대상 사용자**: **로그인한 동아리 회원 전용**
- **목적**:
  - 공연 기본 정보(제목, 일시, 장소)와 **선곡회의 일정/장소**를 포스터 없는 카드로 확인.
  - 해당 공연에 대한 본인의 참여 여부(`참여`, `불참`, `미정`) 및 희망 세션 파트, 전달 메모를 제출 및 수정.
- **보안 및 권한 정책**:
  - **비회원(비인증) 접근 완전 차단**: 비로그인 사용자가 직접 링크로 접속할 경우 공연 및 선곡회의 정보가 전혀 노출되지 않으며, 서버 사이드에서 즉시 로그인 페이지(`/auth/login?redirect=/gigs/[id]/join`)로 리다이렉트됩니다.
  - 로그인 완료 후 원래 접근하려던 참가 신청 페이지로 자동 복귀합니다.

---

## 2. 디자인 및 화면 레이아웃 (Layout & Visuals)

```
+---------------------------------------------------------------+
| 공연 참여                         [승인 대기 중: 기존 참여 신청] [X] |
| 공연 제목 / 부제목                                              |
| 공연 일시·장소 / 선곡회의 일시·장소                              |
|                                                               |
| 참여 여부 *     [참여]       [불참]       [미정]                 |
| (최초 신청·무시 후에는 미선택)                                  |
|                                                               |
| 참여 선택 시 세션 *                                            |
| [보컬(남)] [보컬(여)] [기타] [베이스] [키보드] [드럼]            |
| [직접 추가한 세션 칩]                                          |
| [세션 직접 입력 (예: 바이올린)                  ] [+ 추가]      |
|                                                               |
| 비고 [                                                     ]  |
|                                            [취소] [저장]       |
+---------------------------------------------------------------+
```

---

## 3. 사용자 인터랙션 및 UX 흐름 (Step-by-Step)

1. **비회원 접근 제어**:
   - 비회원이 카카오톡 등 단톡방에 공유된 링크를 클릭하여 접속하면, 본문이 렌더링되기 전에 `/auth/login?redirect=/gigs/:id/join`으로 즉시 튕깁니다.
   - 로그인이 완료되면 즉시 본 페이지로 되돌아옵니다.
2. **공연 및 선곡회의 일정 확인**:
   - 상단 카드에서 공연 타이틀, 공연 일시뿐만 아니라 **선곡회의 일시**와 공연/회의 장소를 확인합니다.
3. **참여 여부 선택**:
   - 최초 신청과 무시된 신청은 아무 상태도 선택되지 않으며 선택 전 저장할 수 없습니다.
   - **참여 (Going)**: 희망 세션을 기본 칩에서 선택하거나 직접 입력한 뒤 `추가`/Enter로 추가해 선택합니다. 빈 입력은 추가하지 않고 같은 이름은 중복 칩 없이 선택합니다. 직접 추가한 세션은 공용 기본 목록에 등록되지 않으며, 저장 후 재접속 시 원문 그대로 복원됩니다.
   - **불참 (Not Going)** 및 **미정 (Undecided)**: 세션 선택은 숨기고 비고 메모(선택)를 작성할 수 있습니다.
4. **신청 제출 및 수정 (`submitGigRsvp` Action)**:
   - `[저장]` 버튼 클릭 시 로딩 스피너 활성화 (`pending === true`).
   - `gig_rsvps` 테이블에 `(gig_id, user_id)` Unique 기준으로 레코드가 UPSERT됩니다.
   - 제출 완료 시 토스트 알림이 노출됩니다. 참여는 **승인 대기**이며, 승인 전에는 상태/세션/메모를 수정할 수 있습니다. 승인 대기 중 버튼은 시계 아이콘으로 표시합니다.
   - 이미 `performers`에 등록된 회원은 신청 다이얼로그 및 서버 액션 재제출이 차단됩니다. 비공개 공연 접근 제한도 서버에서 확인합니다.
5. **관리자 확인 연동**:
   - 관리자 페이지(`/admin/members`)의 **[참가 신청 현황]**에서 `공연제목 | 이름 | 기수 | 신청세션 | 비고 | 승인 | 무시` 순서의 한 줄 표로 표시합니다. 승인 대기 칩은 제거하며 미정 응답은 명단과 집계에서 모두 제외합니다.
   - **초록 체크(승인)**: 신청한 세션으로 공연자 등록 후 LINEUP에 반영합니다. 신청자의 `공연 참여` 버튼은 초록 체크로 비활성화하며 히어로/모바일 양쪽에서 사라지지 않습니다.
   - **빨간 휴지통(무시)**: RSVP를 삭제합니다. 신청자는 미신청 버튼과 미선택 다이얼로그로 돌아가 다시 신청할 수 있습니다.
   - 승인/무시는 `going` 신청에만 제공하며, 승인된 회원은 대기 명단에서 제외합니다. 변경되거나 이미 처리된 신청은 중복 처리하지 않습니다.
   - 다른 사용자의 열려 있는 화면은 재방문/새로고침 시 갱신됩니다.
6. **다이얼로그 닫기**:
   - 취소, 닫기, 배경 클릭 또는 Escape로 닫습니다. 저장하지 않은 내용은 다음에 열 때 저장된 RSVP 상태로 초기화합니다.

---

## 4. UI 상태 (States)

| 상태 | 화면 표시 및 처리 |
| :--- | :--- |
| **비로그인 (Unauthenticated)** | 서버 사이드에서 로그인 페이지로 즉시 강제 리다이렉트 (정보 은닉) |
| **최초 접속 / 무시 후** | 참여 여부 미선택, 프로필 파트 자동 주입, 상태 선택 전 저장 불가 |
| **재접속/수정 (Existing RSVP)** | 이전 선택 상태/파트/메모 복원, `[저장]` 버튼 |
| **승인 대기** | 시계 아이콘, 신청 수정 가능 |
| **공연자 등록 완료** | 초록 체크 아이콘과 비활성화된 `공연 참여` 버튼 유지, 다이얼로그 열기 차단 |
| **제출 중 (Pending)** | 버튼 비활성화 및 로딩 스피너 작동 |
| **제출 완료 (Success)** | 토스트 알림 및 최신 데이터 반영 |

---

## 5. 관련 소스 코드 파일
- 페이지 라우트: [app/gigs/[id]/join/page.tsx](file:///d:/dev/sokna/app/gigs/[id]/join/page.tsx)
- 신청 다이얼로그: [components/gigs/gig-join-dialog.tsx](../../components/gigs/gig-join-dialog.tsx)
- 관리자 승인/무시 UI: [components/gigs/gig-rsvp-manager.tsx](../../components/gigs/gig-rsvp-manager.tsx)
- 참가 신청 액션: [app/gigs/actions.ts](file:///d:/dev/sokna/app/gigs/actions.ts) (`submitGigRsvp`)
- 데이터 모델 및 타입: [lib/gig.ts](file:///d:/dev/sokna/lib/gig.ts) (`GigRsvp`)
