# 공연 공개 범위 명세

> 작성일자: 2026-09-26
> 상태: 구현 및 운영 DB 적용 완료 (2026-09-26)

## 1. 목적
공연의 공개 범위를 `비공개 | 회원 공개 | 전체 공개`로 구분합니다. 기존 비공개 공연의 로그인 회원 열람 범위는 회원 공개로 유지합니다.

## 2. 공개 범위와 권한

| 값 | 화면 표시 | 비로그인 방문자 | 로그인 회원·공연 참여자 | 관리자 |
| --- | --- | --- | --- | --- |
| `private` | 비공개 | 조회 불가 | 조회 불가 | 조회 가능 |
| `members` | 회원 공개 | 조회 불가 | 조회 가능 | 조회 가능 |
| `public` | 전체 공개 | 조회 가능 | 조회 가능 | 조회 가능 |

- 목록, 상세, 페이지 메타데이터에서 동일한 `canViewGig()` 규칙을 적용합니다. 비공개 공연은 참여자로 배정되어 있어도 관리자만 조회합니다.
- 권한이 없으면 공연명·장소·셋리스트·출연자 등 내용을 제공하지 않습니다. 로그인으로 열람 가능한 회원 공개 공연은 로그인 안내를 제공합니다.
- 생성·수정은 관리자 전용입니다. 새 공연의 기본 공개 범위는 기존 동작과 동일한 **회원 공개**입니다.
- 회원 공개/전체 공개 공연에는 로그인 회원이 참가 신청할 수 있습니다. 실제 공연자는 기존대로 재신청할 수 없습니다. 비공개 공연에는 관리자가 아닌 회원의 신청을 차단합니다.
- 선곡회의는 공연을 조회할 수 있는 사용자 중 해당 공연 참여자와 관리자만 이용하며, 후보곡 마감 및 수정 권한 규칙을 유지합니다.

## 3. 데이터 모델 및 전환

- `gigs.visibility`: `text NOT NULL DEFAULT 'members'`, CHECK 값은 `private`, `members`, `public`.
- 기존 `is_public=false`는 `members`, `true`는 `public`으로 일괄 전환합니다. 기존 공연을 새 관리자 전용 비공개로 자동 변경하지 않습니다.
- `is_public`은 호환용으로 유지하며 트리거가 `visibility='public'` 여부로 동기화합니다. 신규 저장·권한 판단은 `visibility`를 기준으로 합니다.
- 마이그레이션 전 행을 읽을 때는 `getGigVisibility()`가 기존 boolean 값을 해석합니다. 잘못된 명시적 공개 범위는 비공개로 취급합니다.
- `gigs` SELECT RLS는 위 권한표를 적용하며, 기존 허용형 정책의 우회를 막는 제한형 정책도 추가합니다. 공연자·확정 셋리스트·후보곡·후보곡 응답의 SELECT에도 상위 공연의 조회 권한을 적용합니다.
- 배포 전 [20260926083828_add_gig_visibility_levels.sql](../../supabase/migrations/20260926083828_add_gig_visibility_levels.sql)을 적용해야 합니다. 기존 마이그레이션 순서를 유지하고 적용 후 `npm run types`를 실행합니다.
- 2026-09-26 사용자 승인 후 운영 DB에 적용하고 `npm run types`를 성공적으로 실행했습니다. 공개 범위 타입은 기존 수동 정의와 일치합니다. 별도 미적용 마이그레이션의 `users.marketing_opted_in_at` 타입은 기존 로컬 기능과의 호환을 위해 보존했습니다.

## 4. 서버 액션

- `createGig`, `updateGig`: 필수 FormData `visibility`를 세 값 중 하나로 검증하고 저장합니다. 누락·잘못된 값 또는 비관리자 요청은 DB 변경 전에 거절합니다.
- `submitGigRsvp`: 회원 공개/전체 공개 공연은 인증된 회원의 신청을 허용하고, 비공개는 관리자만 허용합니다.
- 공개 범위 수정 후 목록·상세·선곡회의 관련 캐시를 갱신합니다.

## 5. 화면

- 공통 `GigForm`에 비공개 → 회원 공개 → 전체 공개 순서의 선택 카드를 제공합니다.
- 선택한 공개 범위는 수정 시 복원되고 폼 이탈 방지 변경 감지에 포함됩니다.
- 목록과 상세에서 `비공개`/`회원 공개` 배지를 구분합니다.

## 6. 검증

- `tests/gig-visibility.test.mjs`: 세 공개 범위와 방문자·일반 회원·참여자·관리자별 목록/상세/메타데이터.
- `tests/gig-visibility-actions.test.mjs`: 생성·수정의 공개 범위 저장·입력 검증·관리자 권한.
- `tests/gig-rsvp.test.mjs`: 회원 공개 참가 신청 허용 및 비공개 거부.
- 타입 검사, 관련 린트, 프로덕션 빌드를 실행하고 외부 인증/환경으로 실행하지 못한 검증은 별도 기록합니다.


## 7. 운영 적용 확인 (2026-09-26)

- 원인: 운영 DB에 `gigs.visibility` 컬럼이 없어 08:32:00 / 08:32:33 UTC에 `column gigs.visibility does not exist` 오류가 발생했습니다. 상세 조회의 기존 데이터 호환 처리와 달리, 신청 액션은 해당 컬럼을 명시적으로 조회합니다.
- 사용자 승인 후 `add_gig_visibility_levels` 마이그레이션을 적용했습니다. 실제 DB 이력 `20260926083828`에 맞춰 로컬 SQL 파일명도 동기화했습니다.
- 기존 공연 4개는 회원 공개 2개·전체 공개 2개로 보존됐고, 공개 범위와 `is_public`의 불일치는 0건입니다. NOT NULL·기본값·CHECK·동기화 트리거를 확인했습니다.
- 실제 REST API에서 `visibility, is_public` 조회가 성공하고 비로그인에게 전체 공개 공연 2개만 반환됐습니다. 읽기 전용 트랜잭션에서 일반 회원·관리자 권한으로 공연 및 참여자 조회가 성공했습니다. 실제 참가 신청 데이터는 검증 목적으로 제출하지 않았습니다.
- 로컬 신청 액션에 컬럼 누락 안내와 조회 오류 로그를 추가했습니다. 운영 DB 변경은 즉시 반영되며 오류 안내 코드 변경은 앱 배포 시 반영됩니다.
